#include<stdio.h>
int main()
  {
     float principle,time,rate,SI;
     printf("enter principle(amount):");
     scanf("%f",&principle);
     printf("enter time:");
     scanf("%f",&time):
     printf("enter rate:);
     scanf("%f",&rate);
     SI=(rinciple*time*rate)/100;
     printf("simple interest=%f",SI);
return 0;
   }
